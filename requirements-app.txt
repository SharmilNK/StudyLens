# Web app dependencies (lightweight — no PyTorch/GPU needed)
# Install with: pip install -r requirements-app.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Gemini API (free tier)
google-genai>=1.0.0

# RAG retrieval (embedding + similarity)
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
numpy>=1.24.0
